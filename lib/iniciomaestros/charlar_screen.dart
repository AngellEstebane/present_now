import 'package:flutter/material.dart';

class CharlarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Charlar'),
      ),
      body: Center(
        child: Text('Contenido de Charlar'),
      ),
    );
  }
}
