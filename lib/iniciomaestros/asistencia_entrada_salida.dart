import 'package:flutter/material.dart';

class AsistenciaEntradaSalida extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Asistencia Entrada y Salida'),
      ),
      body: Center(
        child: Text('Página de Asistencia Entrada y Salida'),
      ),
    );
  }
}
