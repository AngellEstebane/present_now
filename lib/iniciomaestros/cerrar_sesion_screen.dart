import 'package:flutter/material.dart';

class CerrarSesionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cerrar sesión'),
      ),
      body: Center(
        child: Text('Contenido de la pantalla de Cerrar sesión'),
      ),
    );
  }
}
