import 'package:flutter/material.dart';

class AvisosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Avisos recientes'),
      ),
      body: Center(
        child: Text('Contenido de la pantalla de Avisos'),
      ),
    );
  }
}
