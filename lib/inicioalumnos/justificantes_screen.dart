import 'package:flutter/material.dart';

class JustificantesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Justificantes'),
      ),
      body: Center(
        child: Text('Contenido de la pantalla de Justificantes'),
      ),
    );
  }
}
