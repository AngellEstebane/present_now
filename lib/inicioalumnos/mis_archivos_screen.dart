import 'package:flutter/material.dart';

class MisArchivosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mis archivos'),
      ),
      body: Center(
        child: Text('Contenido de Mis archivos'),
      ),
    );
  }
}
